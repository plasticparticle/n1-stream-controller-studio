{
  "name": "n1-stream-controller-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Linux configuration studio for the TreasLin VSDinside N1 stream controller",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "npm run native:prepare && node --check app.js && python3 -m py_compile driver/n1_service.py && bash -n install.sh scripts/*.sh && cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo test --manifest-path src-tauri/Cargo.toml",
    "frontend:build": "bash scripts/build-frontend.sh",
    "sidecar:build": "bash scripts/build-driver-sidecar.sh",
    "native:prepare": "npm run frontend:build && npm run sidecar:build",
    "setup:driver": "bash scripts/setup-driver.sh",
    "setup:udev": "bash scripts/install-udev.sh",
    "driver:probe": ".venv/bin/python driver/n1_service.py --probe"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
